{
  "name": "retrospective-insight-ai",
  "version": "1.0.0",
  "description": "Full-stack application for AI-powered retrospective insights",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd server && npm install",
    "build:full": "cd server && npm install && cd ../client && npm install && npm run build",
    "start": "node server/index.js",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["azure-devops", "react", "nodejs", "api"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "helmet": "^7.1.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.1"
  }
}
